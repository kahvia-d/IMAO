# 罗伊冰原地图补充

本轮输入为 NVIDIA 目录中 21:47:16–21:49:18 的拉海洛四组截图，以及 21:56:49–21:58:15 的黯原四组截图。原图不修改；源文件哈希、四点坐标、回放清单和诊断结果保存在 `out/roy-maps-20260909/`。

## 地图身份和资源

| 地图 | 场景 / state | 下载瓦片 | 特征数 |
|---|---|---:|---:|
| 拉海洛 | Lahai / 906 | 110 | 256900 |
| 黯原 | Darkplain / 909 | 25 | 37524 |
| 冰原地表 | World / 8，包名 RoySurface | 63 | 68018 |

数据来自库街区公开瓦片，资源版本 `E62CEAC5F80745288BF76C7AD5F731C3`。范围依据区域元数据、点位主体分布和截图选择，不追随归档点位中明显越出区域的极端坐标；未修改归档点位。瓦片范围分别为 x=-6..4/y=3..13、x=-1..4/y=-2..3、x=-4..3/y=4..12；公开源不存在的瓦片仅在 HTTP 404 时跳过。

## 坐标及比例

拉海洛四点游戏坐标：(-3860,-6491)、(-667,-7444)、(732,-5152)、(-798,-5287)。大地图校准最大误差 0.303 像素。

黯原四点游戏坐标：(-214,799)、(-196,-172)、(662,-250)、(1179,616)。大地图校准最大误差 0.202 像素。截图中第三个坐标分量没有参与平面坐标校准。

拉海洛还有独立于大地图变换的小地图比例差异：四张截图的 RANSAC 相似变换比例分别为 2.14773、2.15384、2.14668、2.14983（内部地图像素 / 184 规范化小地图像素）。中位数 2.1487797427。此前固定的 194/184 比例使四张小地图都被拒绝。

新增按场景读取 `minimapScale`，用于全局几何检查、平移投票、局部跟踪、地图关闭后的地形参考和标记投影。保留原有相对比例容差、几何内点、残差、歧义和连续帧确认要求。未配置的地图继续使用原比例。资源预检同时核对包中已验证比例与所选地图校准一致。

旧基础库含有被归入 World 的拉海洛特征。描述子配准支持 48981 个内点，中位位移为 (-1.328125,-9.791016)；经过图集连通分量检查，109540 行由新包替代。排除信息绑定基础特征和替代特征的哈希，只随已加载的新包生效。注册工具新增可选空间候选半径，避免对大型地图做全部描述子的笛卡尔积；本轮候选半径 150，最终配准门槛仍为至少 12 个内点、35% 分量支持、2 像素残差。

## 验证边界

黯原使用原始截图回放进行本地试用开放，`validationScope=local-capture-trial`，没有把连续跟踪或运行中浮层验收写成通过。原有完整实机发布流程保留；本地试用入口检查原图及回放报告哈希，篡改报告会被拒绝。

用户补充了 22:25:17/22:25:21 的冰原地表截图，地点为盲望之塌，游戏坐标 (-1531,-6279)。原 World 变换及小地图比例通过参考验证；地表包已加入实际运行快照。该点是既有坐标系的单点核对，不是重新拟合 World 或全区域实机验收。

四张独立小地图分别来自不同位置，不能充当连续视频，也不能把原始匹配数当作已发布位置数。最终回放与实际快照检查结果见本目录下的本轮输出记录。

## 最终结果

- 三张新增地图的 9 张大地图均通过：拉海洛 4/4、黯原 4/4、地表 1/1。
- 使用实际启动快照，无坐标提示的小地图分批搜索恢复：拉海洛 4/4、黯原 4/4、地表 1/1。最大位置误差分别为 6.650、3.668、3.150 内部地图像素，均小于原 8 像素标准，未发现错误场景。
- 单次搜索预算不足时会返回未完成或拒绝；分批回放复用了同一原始截图推进搜索，不伪造第二帧、不作为发布位置或连续跟踪证据。地表样本需要超过 24 次搜索尝试，完整离线恢复约 1.17 秒。游戏中处理新帧的耗时与此不同。
- 39 项多场景合成测试、OptimizationTests 通过；新增两倍小地图比例跟踪及错误比例拒绝用例。World 大地图 8/8、29 帧小地图回归通过；泰缇斯四张大地图及原 30 帧视频通过，后者仍为 26 帧正确强匹配、13 次正确发布、错误发布 0。
- 原 F8 版本的托管回归针对当前 CoreHost 通过；首次受限环境运行被本地管道权限阻止，在独立测试数据目录重跑通过，未使用真实完成记录做测试。
- 已通过当前安装的 ResourceSnapshotService 确认实际启用内置快照，包列表同时包含 Lahai、Darkplain、RoySurface，CoreHost 的 resourcesReady、viewportReady、visualReady 均为 true。F8 界面文件保持原安装版本。

本轮未提交、未推送或发布。实际游戏中的连续移动、场景切换与浮层验收仍需重启工具后确认。
