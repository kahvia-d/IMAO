# 小地图局部覆盖层（Mini Overlay）正式设计

> **状态：设计待评审，代码未动。** 依据是 `Docs/MinimapMarkerRenderPerfPlan_zh-Hans.md` §28 的实机 A/B/A/B 结果。
> 本文是"现行方案"，评审通过后随实现一起更新。

## 1. 已证结论（本轮实验数据）

同一次运行内 A/B/A/B（`full(0)` / `mini(128)`），场景基线 102 fps：

| | full | mini | 差 |
|---|---|---|---|
| FPS | 102.1 / 103.4 | **111.9 / 112.8** | **+9.6**（相对 ≈ +9.3%） |
| p95 | 12.49 / 12.04 | 9.84 / 9.75 | **−2.4 ms** |
| p99 | 14.50 / 14.33 | 10.86 / 10.65 | **−3.7 ms** |
| >20ms | 0.09% / 0.02% | **0 / 0** | → 0 |
| 窗口 / backbuffer | 2560×1440 | **355×344** | 面积的 3.3% |
| `segPresentMs` | 1.185 / 0.866 ms | **0.423 / 0.438 ms** | Present 长尾 40/32 ms → **2/1 ms** |
| `markerDrawMs` | 0.024 ms | 0.024 ms | 相同（同 1380 次绘制） |

机制：**收益在合成/present，不在 marker 绘制**；不是"少画了 marker"，也不是"少刷新了"（present 次数没减少）。
PresentMode 四段全是 `Composed: Flip` —— **不是**恢复了独立翻转。

## 2. 目标与非目标

**目标**：把"普通探索时用小窗口、开大地图用整屏"变成默认行为，且**不改变**坐标、定位、marker 语义与交互。

**非目标**（本轮明确不做）：重构大地图覆盖层、把 DirectComposition 变成默认、Texture Atlas、WGC ROI Readback、更换 GUI 框架。

## 3. 切换规则

| 状态 | 窗口 | 依据 |
|---|---|---|
| 普通探索（小地图可见、大地图未开、点位显示开） | **mini**（小地图矩形 + padding） | 已有 `frame.minimapVisible && !frame.mapVisible` |
| 大地图打开 | **整屏**（现状不变） | `frame.mapVisible` |
| 定位不可信 / 小地图缺失 | 保持 mini 尺寸但**不画 marker** | 现有 `ClearNearItemsData` 路径不变 |
| 游戏窗口最小化 / 失焦 | 沿用现状（`overlay-window-visibility` 的 idle 隐藏等） | 不改 |

窗口尺寸**只由游戏客户区尺寸 + DPI 决定，不由内容决定**：marker 有无、marker 数量都不触发改尺寸，避免尺寸抖动。

## 4. 几何与坐标系

沿用已实测通过的实现（`ImGuiOverWindows.cpp`）：

```text
MiniOverlayRect = (小地图矩形 + padding) ∩ 游戏客户区
padding         = clamp(客户区宽 × 0.025, 30, 60) px
```

- 小地图矩形来自 `GameWindowsScreenData::MinMapScreenData` 按客户区缩放（与其余 HUD 同一套换算）。
- **2560×1440 实测 = 355×344**，与公式预测一致。
- 绘制空间**仍是游戏客户区**：所有 marker / 路线 / 提示坐标含义不变；把顶点平移进小窗口（**不能**用 `DisplayPos`——本仓库 ImGui 把主 viewport 位置硬编码为原点，非零偏移会裁错）。
- 窗口跟随与缓冲区尺寸**复用** `OverlayWindowBounds`（未移动不调 `SetWindowPos`、按实际几何回读）与 `OverlayBackBufferSize`（自动把 SwapChain 调成窗口客户区）。
- 窗口样式不变 ⟹ **仍然完全鼠标穿透**。

## 5. 决策项（D1 已定，D3/D4 待定）

### D1（已决策，2026-09-21）游戏内状态栏：默认"状态球"，可选"完整栏"

**决定**：把状态栏缩成一个小**状态球**，放在小地图矩形的右下角，平时只用**颜色**表示当前状态；另给一个设置开关切换两种模式——**极简（默认）** 与 **非极简（今天的样子）**，后者同时把窗口扩成并集矩形，留给需要诊断信息的时候用。

| 模式 | 窗口 | 画什么 |
|---|---|---|
| **极简（默认）** | mini：小地图 + padding（2560×1440 → **355×344，3.3%**） | marker + 右下角状态球 |
| **非极简** | **并集**：mini ∪ 状态栏矩形（→ 约 **1505×344，14%**） | marker + 今天的完整状态栏 |
| 大地图打开 | 整屏（不变） | 不变 |
| `statusBarEnabled = false` | mini（纯 marker） | 不画球也不画栏 |

推导出的细节：

- **球的位置**：圆心在小地图矩形右下角内缩一个半径处。2560×1440 的小地图矩形是 48..294 × 37..283，球半径 ≈14 px ⟹ 圆心约 (280, 269) —— **完全落在现有 60 px padding 内，几何不用改**。
  - 半径 = `max(8, 客户区宽 × 0.011 / 2)`（与 marker 半径同源，随 DPI/分辨率缩放）。
- **球的颜色**：直接复用 `RuntimeStatusBar` 已有的状态色（正常 / 恢复中 / 暂留 / 故障各有其色），**不造第二套状态判定**。
- **球跟随 `statusBarEnabled`**：玩家把游戏内状态栏整体关掉时，球也不画——那时只剩 marker，窗口最小。
- **并集以稳定开关为条件**，不跟每帧的可见性走 ⟹ 尺寸在一个配置下恒定，不会抖动。
- ⚠️ 颜色是唯一编码，对色觉障碍不友好。后续可选：故障/恢复态额外画一个感叹号。**本轮按你的要求先只做颜色。**

**控制路径分两步**（关键：第一步不需要托管重建）：

1. **现在就能用**：把隔离掩码 **bit 7（`128`）** 的语义定为「**完整状态栏模式**」（并集窗口 + 完整栏）；不设 = 极简（新默认）。
   - 回滚到今天的行为不再依赖 bit，而是**换回旧 CoreHost**（`C:\Dapps\IMao\IMao-CoreHost.before-perf-phase12-20260921.exe`，见台账 §26.5）。
2. **稍后（托管侧）**：设置页加"状态栏样式：极简 / 完整"开关，值随现有 preferences 命令下发（与 `statusBarEnabled` 同一条路：`RuntimeConfiguration` → `configure` 命令 → `ApplyConfigure`）。生效值 = **设置 OR 诊断位**（位用于诊断时强制完整）。
   - ⚠️ 这一步需要**托管重建**，而本环境托管 shell 无法干净重建（`IMao-WinUI.csproj:161` 的既有 `MSB4186`，台账 §26.1.1）。要落地得先修那条构建问题，或在能正常构建的机器上出这一版。
   - 诊断页 `128` 的说明文字也要随之改成"完整状态栏模式"（走已验证过的 `resources.pri` 部署路径，§26.1.2）。

### D2 隔离开关与默认行为

- **bit 7（`128`）= 完整状态栏模式**（见 D1）：设上 = 并集窗口 + 完整状态栏；不设 = 极简（新的生产默认）。
- 不新增 bit（那个 NumberBox 上限刚改成 255，bit 0–7 已用满）。
- **回滚**：换回旧 CoreHost（备份文件已在 `C:\Dapps\IMao\`），或在能构建的机器上 `git revert`。不再为"强制整屏"单独占一个 bit。
- **不做**（本轮）：设置页的新开关属于 D1 第 2 步，需要托管重建，环境受限时延后；不新增用户可见设置项以外的任何偏好字段。

### D3 要不要"有 marker 才 mini"

**建议不要**。窗口尺寸跟内容走会在地图开合、玩家走动时反复改尺寸；空的小窗口是透明的，成本可忽略。

### D4 切换瞬间要不要去抖

**先测再决定**。大地图开/关会各触发一次 `ResizeBuffers`（`Ensure` 路径，之后 `hasPresented=false` 强制重画一帧）。要确认的是：切换前后是否有肉眼可见的一帧错位或闪烁、以及切换期间是否丢 marker。若一帧就位就不加去抖；若出现闪烁，再加"小地图状态稳定 N 帧后才缩窗口"的小延迟。

## 6. 切换过程的行为与风险

| 项 | 结论/待办 |
|---|---|
| 换尺寸的代价 | `OverlayBackBufferSize::Ensure` → `ResizeBuffers` → 强制一帧 present（现有路径，与游戏窗口缩放同一条代码） |
| 定位锚点保留 | `CanRetainOverlayAnchor` 比较的是**游戏客户区**尺寸，与覆盖层窗口大小无关 ⟹ 不受影响 |
| `SetWindowPos` 频率 | 只在矩形变化时调用（复用的 helper 本来就"未变不调"）；不用新增高频调用 |
| 非 16:9 / 窗口部分移出屏幕 | 矩形按客户区各自缩放并**裁剪在客户区内**；不越出游戏窗口 |
| 大地图路径 | **一处不动**（`mapVisible` 时回到整屏，与今天完全一致） |

## 7. 回归与验证计划

1. **重场景 A/B 复测**：在你报"掉帧 10–20 FPS"的场景（基线 26–60 fps）跑同样的 `A/B/A/B`，确认**相对**收益成立（本次绝对 +9.6 fps 属于 102 fps 的轻场景，不能外推）。
2. **D1 方案 A 的面积代价测量**：并集窗口（14%）下的 `A/B/A/B`。
3. **切换行为**：连续开关大地图 10 次，检查日志 `overlay-window-position-error` / `overlay-backbuffer-pending` / `minimap-overlay-drop` 均为 0，且肉眼无闪烁。
4. **DPI 100% / 125% / 150%**：marker 与状态栏位置、窗口位置逐档核对（未验证项）。
5. **任务书 §16 全量回归**，重点：3/4（完成态刷新）、6（大地图 marker）、9（切图后 Overlay）、11（DPI）、12/13（Alt-Tab/最小化）、14（穿透）、15（无残留 HWND）、16（设备重建后纹理索引重建）。
6. **长时间运行**：≥30 分钟，确认没有窗口尺寸漂移或内存增长（未验证项）。

## 8. 落地顺序（每步独立提交、独立回滚）

| 步 | 内容 | 状态 |
|---|---|---|
| S1 | 原生：极简状态球 + bit 7 改为"完整状态栏模式"（并集窗口） | ✅ 已实现、编译验证（`52a6fc1`）；**实机外观未验证** |
| S2 | 默认翻转（mini + 球成为小地图状态下的默认） | ✅ 与 S1 同一次提交落地；**实机回归未做** |
| S3 | 托管：设置页"状态栏样式"开关 | ⏳ **未做**（需要能干净构建托管 shell；见 §26.1.1 的 `MSB4186`）。诊断页文案与掩码上限已更新（`c3d0fc2`） |
| S4 | 重场景 A/B 复测 + DPI 100/125/150 + 连续开关大地图 10 次 + 长时间运行 | ⏳ 未做 |

### 8.1 已实现的开关语义（诊断页那个框）

| 掩码 | 含义 |
|---|---|
| `0`（默认） | mini 窗口 + **状态球**（极简） |
| `128` | **完整状态栏**：窗口扩成并集（2560×1440 下约 1505×344） |
| `256` | **强制整屏覆盖层**：回到"今天"的整屏窗口（对照与回滚） |
| 组合 | 允许（如 `384` = 整屏 + 完整栏） |

- `128`/`256` 立即生效；`64` 仍需下次「开始探索」。
- 大地图打开时总是整屏覆盖层。
- **回滚**（不重新构建）：把 `C:\Dapps\IMao\IMao-CoreHost.before-perf-phase12-20260921.exe` 复制回 `IMao-CoreHost.exe`；或把掩码设为 `256`（只回滚窗口尺寸，球/栏样式仍按新逻辑）。

### 8.2 已知产物与限制（实现后新增）

| 项 | 说明 |
|---|---|
| 通知（`Notification::DrawInfo`） | 它用 ImGui 窗口，位置取自"视口右下角"。mini 模式下视口就是小窗口 ⟹ 通知会出现在小窗口右下角：**可见但很窄**（长句会折行）。要改的话是"让通知在小地图附近排版"，属后续项。 |
| ImGui 窗口在非零偏移下的错位 | 顶点平移按客户区坐标做，而 ImGui 窗口按视口空间排版；两者在 `miniClient` 偏移非零时差一个偏移量。现实里偏移只在**客户区宽 > 3200 px** 时出现（约 12–15 px），≤3200 px 时为 0。当前 HUD（小地图在左上角）不受影响。 |
| 状态球只有颜色 | 对色觉障碍不友好（§5 D1 已记）。 |
| 极简模式下**看不到文字提示** | 例如冷启动的"请打开一次大地图以确定所在区域"。**D1b 已定（2026-09-21）：不做**——保持纯颜色。后果：这条提示只在主窗口或切到完整状态栏模式（掩码 `128`）时可见；球在恢复/暂留/故障态用颜色区分。 |

## 9. 明确不做

- 不重构大地图覆盖层；不把 DirectComposition 设为默认；不删 layered fallback。
- 不为了这次切换引入 manager/service/controller 层。
- 不改定位算法、marker 完成语义、路线语义、用户数据格式。
- 不动 SIFT/SURF/OpenCV 阈值。
