# Real WinUI guide-window integration harness

This executable links the production `MarkerGuideWindow`, `GamepadAssistantWindow`, `MarkerGuideCoordinator`, `GamepadInputService`, `XInputControllerReader` and guide/route/input models. The CoreHost and detail service are replaced with narrow in-memory fakes. The default service test injects only controller samples while retaining the production dispatcher timer and action routing. It creates actual WinUI windows on the current interactive desktop, then closes all test windows and exits. It never starts CoreHost, connects to the game, reads user progress, sends synthetic keyboard input, or accesses the network.

The assertions inspect the real `OverlappedPresenter`, `AppWindow.IsVisible`, native `WS_CAPTION` and `IsWindowVisible`. They also compare `ClientToScreen(0,0)` with `GetWindowRect` and DWM extended frame bounds: both measured top gaps must be zero. A title-bar flag alone does not establish that there is no visible nonclient strip. Controlled asynchronous replies exercise registration, local/online loading, authoritative route lookup, completion acknowledgement and marker event ordering. F8 and paging are injected at the native protocol event boundary; this does not test the global keyboard hook itself. `CompleteCurrentAsync` exercises the completion-button save path, while refresh and enlarge tests invoke the real controls' automation providers.

Build from the repository root in the configured Visual Studio developer environment. The Windows App SDK version matches the main application, and output stays outside the running application directory:

```powershell
. .\scripts\Enter-DevEnvironment.ps1
dotnet restore .\Tests\GuideWindowRuntime\GuideWindowRuntime.csproj -p:Platform=x64 -p:NuGetAudit=false --ignore-failed-sources
.\Tests\GuideWindowRuntime\Build.ps1
```

The build script locates desktop MSBuild and explicitly selects the repository's .NET SDK. `EnableMsixTooling=true` selects the Windows App SDK's bundled PRI tasks; `WindowsPackageType=None` still produces an unpackaged test executable. Disabling MSIX tooling incorrectly falls back to missing legacy Visual Studio PRI tasks on this machine.

Run only in an interactive Windows desktop session. The test briefly shows its own guide windows. This command starts only the test executable; its manifest uses `asInvoker` and requires no elevation:

```powershell
$guideTest = Start-Process -FilePath (Resolve-Path .\out\guide-window-runtime\GuideWindowRuntime.exe) -WindowStyle Hidden -PassThru
if (-not $guideTest.WaitForExit(60000)) {
    Stop-Process -Id $guideTest.Id
    throw 'Guide window integration tests timed out.'
}
$guideTest.Refresh()
Get-Content .\out\guide-window-runtime\guide-window-tests.log
if ($guideTest.ExitCode -ne 0) { throw 'Guide window integration tests failed.' }
```

The log contains one PASS line per scenario, real HWND/style evidence, and the final success line; exceptions result in a nonzero process exit code. This harness intentionally is not part of the default headless `Test-Runtime.ps1` workflow. A successful run establishes real-window behavior with controlled services, not real-game keyboard routing or external guide content availability.

Verified on 2026-09-08: 25 scenarios passed with process exit code 0 (24 real-window scenarios and one placement calculation group). They include physical game-left placement, 100/125/150/200 percent DPI calculations, negative coordinates, small game windows, image-page boundaries, stale paging events, image-to-empty refresh, and a visible enlarged-image failure message with the correct page number. Image fixtures are generated locally under the harness output directory; no image URL is fetched.

Four gamepad scenarios use a test-owned foreground window as the game context and call production semantic actions: cancelling a pending target reply cannot resurrect an assistant; overlapping candidates retain separate identities and only the selected point completes; losing focus during asynchronous detail registration cannot activate a late guide; an unknown map scene suppresses nearby candidates while preserving an explicitly identified route target. The list scenario also verifies that a mouse-overlay selection-cleared event does not close the frozen gamepad list. A fifth scenario injects neutral input followed by RB press and release into the real `GamepadInputService`, then verifies that its actual dispatcher timer does not open on press and performs target query, HWND registration and assistant activation exactly once on release. The default regression never reads a physical controller; `Tests/ManagedRuntime/GamepadInputTests.cs` separately covers broader timed interpreter boundaries. The foreground scenarios require a normal interactive desktop: a restricted process may display windows yet be unable to activate the test-owned window, which is an environment failure rather than a passing focus check.

`--test-gamepad-service` runs only the complete production-service entry scenario and writes `out/guide-window-runtime/gamepad-service-tests.log`. `--probe-gamepad-reader` is a separate diagnostic mode that does read the real controller through the production reader: it records four slots for one second while initially in the background, one second with its own small window foreground, and one second after restoring the original foreground. It records raw samples and native packet numbers in `out/guide-window-runtime/gamepad-reader-probe.jsonl`; it never emits controller events or changes game settings. On the 2026-09-08 run, slot 0 remained connected with identical nonzero neutral stick offsets in all three phases, and the original foreground was restored. No button press occurred during that probe, so it does not establish whether a real background LB gesture reaches the main application. The injected service scenario passed separately and is not real-game integration evidence.

`--test-gamepad-cross-process` runs three additional scenarios against a separate test-owned WinUI process, using the real service timer and `GamepadWindowActivation` helper. It verifies that a fresh RB release transfers foreground from that independent process to the assistant, an unresponsive source cannot produce a false successful opening, and moving foreground to another window during a pending target reply is respected. The source deliberately pauses its own UI thread for one second in the failure case; no real game is controlled. All three passed again after the visibility changes on 2026-09-08, as did all 25 default scenarios. The successful transfer required the helper's bounded thread attachment after the normal activation request was denied; source timeout returned `source-probe-failed` and `opened=False`. The current log is `out/guide-window-runtime/gamepad-cross-process-tests.log`; `out/gamepad-cross-process-baseline/fixed-regression.log` preserves the earlier hold-entry run.

`--test-assistant-visibility` adds four groups: DPI/work-area placement calculations (including negative monitor coordinates), a real cross-process opaque topmost coverage reproduction, the fixed visible assistant, and rejection/recovery of an off-screen foreground window. On 2026-09-08 at 19:07, a normal assistant owned foreground and had laid-out content (505.6 × 612 DIP), but all five client samples were covered by the independent topmost source. Setting the production topmost presenter exposed all five samples, and a bounded desktop screenshot showed the heading, selected point and B/Y controls. Evidence is in `out/guide-window-runtime/assistant-visibility-20260908-190711`, with `01-foreground-but-covered.png` and `02-assistant-visible.png`; the summary is `assistant-visibility-tests.log` in the harness output directory. Real desktop DPI was 120; physical multi-monitor hardware was not available, so negative coordinates are a calculation test. The helper now logs styles, DPI, work area, bounds, layout and sampled covering HWND/PID instead of equating foreground with visibility. Hit testing is a sampled observation, not a guarantee about every pixel of transparent overlays; these opaque-surface screenshots validate the reproduced mechanism. The old 18:47 game log recorded foreground success without geometry or covering-window evidence, so this controlled reproduction does not identify retrospectively which real window covered that session.

`--test-route-controller` runs six groups against the production route controller, transparent HWND and guide coordinator. It checks a real 1 × 1 pixel, alpha=1 foreground input host; deferred A/B press/release ordering and B return; cancelling pending input after foreground loss; cancelling after a 225 ms sampling gap; standalone guide RB/LB paging and B close; explicitly choosing the second nearby candidate; and a toolbar `sourceHwnd` handoff into a visible guide without the ending toolbar taking focus back. Some related checks share a group. All six passed on 2026-09-08 with zero completion writes; the log is `out/guide-window-runtime/route-controller-tests.log`. Native IPC replies are controlled in-memory acknowledgements, so this does not validate the real native toolbar widgets or game input consumption. The transparent host deliberately uses the activation helper's basic HWND visibility check; it is not expected to expose visible content. The latest combined results are 25 default groups, 3 cross-process groups, 4 visibility groups, and 6 route groups, with zero build warnings/errors. The visibility suite was repeated at 19:13, preserving a second screenshot pair in `assistant-visibility-20260908-191310`.

The original coordinator reproduced `opened=True` while foreground still belonged to the independent source, immediately followed by `assistant-unfocused`. The first run's captured diagnostic excerpts are preserved in `out/gamepad-cross-process-baseline/baseline-first-run-tool-capture.txt`; the original full log was overwritten by a later preparation failure, so `baseline-reproduction.log` is not evidence of that initial reproduction. `Build.ps1 -GamepadBaseline` can link the preserved coordinator/service snapshots in that evidence directory. `first-fixed-lock-observation.log` preserves an intermediate run showing that `LockSetForegroundWindow` did not reliably deny the attached activation on this desktop; the final negative case uses the bounded unresponsive source instead. These checks validate Windows foreground behavior with controlled processes, and do not replace a real-game retest.

The earlier title-bar-only check missed a 9-pixel visible nonclient strip at 125% DPI despite `HasTitleBar=false` and `HasBorder=false`. Recorded API comparisons found 3 pixels after disabling resize, 9 after extending content into the title bar, and 8 after clearing frame style bits. The production `WM_NCCALCSIZE` handling reduces both actual top gaps to zero and keeps them zero after hide/reopen. Final native style is still `0x144C0000`; the geometry assertion, rather than that style alone, proves this fix. The comparison log is `out/guide-window-frame-comparison.log`.

The final 2026-09-08 window-return revision passed 48 groups: 25 default, 6 route, 3 cross-process activation, 7 assistant visibility, and 7 cross-process return/handoff. `--test-gamepad-return` writes `gamepad-return-tests.log`. A separate simulated game process runs above a visible simulated application main window, with foreground events recorded throughout the action. The cases cover two observed game-foreground confirmations before disposing the source, an unresponsive target leaving an inert host, a full neutral/B-release barrier before an explicit retry, an intentionally stale saved PID against a real HWND, a destroyed game, and preserving another foreground during return. The production service timer also drives a delayed toolbar-to-guide handoff and verifies that the source stays alive until guide activation is confirmed; AltTab during registration cancels the late guide. These fixtures do not use a physical controller or the actual game.

Assistant visibility now also checks actual client/outer/DWM top-edge equality at 125% desktop DPI, drags the content heading using the pointer against only the verified test-owned foreground window, and invokes the rendered content close button. The close button remains separate from the draggable title region. The successful screenshot shows the assistant without system title bar or white frame above the independent opaque topmost surface. Off-screen recovery is tested before pointer dragging so the operating system's last drag placement cannot restore the fixture during activation. Final evidence is preserved in `out/gamepad-window-return-20260908/final`; the source screenshot pair remains in `out/guide-window-runtime/assistant-visibility-20260908-202648`. The isolated harness build completed with zero warnings and errors.

The later real-game report exposed a coverage gap in those first return tests: their normal `SetForegroundWindow` requests happened to succeed. The strengthened return fixture uses `LockSetForegroundWindow` in the verified foreground test host to obtain a genuine Windows refusal, with no mocked native API result. It requires `RequestAccepted=False`, actual thread attachment and successful detachment, and two observations of the original independent game in foreground before source disposal. Attaching queues can itself finish queued activation; `AttachedRequestMade` distinguishes that case from an explicit attached request. The recovered-game retry case retains the lock, so a successful retry also has to use the attachment path. The foreground-change case verifies cancellation before attachment when the user selects another real window. `--probe-gamepad-return-refusal` separately records the raw normal/attached API observations and always releases its test-owned foreground lock. Evidence for this revision is kept separately in `out/gamepad-return-fallback-20260908`; the earlier 48-group logs remain unchanged. These tests cover the observed denial path, not a retest of the actual game.

The strengthened seven return groups passed with the final fallback helper, followed by all 25 default and six route groups. In the first locked return, the normal request was refused, attachment completed queued activation, detach succeeded, and two foreground observations confirmed the game after 109 ms. The unresponsive target stayed stopped at its source window; a fresh B release after recovery returned through attachment after 93 ms. Selecting another window after normal refusal cancelled without using attachment. Display-only `returning/failed/returning` messages were also verified to follow native input termination with the original session ID, without sending any new native action samples. Final logs and source hashes are in `out/gamepad-return-fallback-20260908/final`; the isolated build reported zero warnings and errors.

`--test-cursor-candidates` runs 11 additional real-window scenarios and writes `cursor-candidate-tests.log`. The first uses the production input-service timer and a fresh RB release to open an assistant containing only a frozen cursor candidate. Other cases cover overlapping candidates, explicit A selection followed by the real interpreter's short/long X boundary, exact state/profile/opaque point IDs, cursor-first deduplication across route and nearby sources, context/filter refusals without fallback, cancellation before a delayed resolver reply, and rejection of old target snapshots or cursor-revision echoes. Delayed name loading also verifies that equal point IDs in different states retain the selected state after the real list repaints.

The cursor fake returns the same `assistantHwnd`, `assistantGeneration` and `cursorRevision` sent to the resolver; the revision fixture exceeds JavaScript's safe integer range. Test cancellation deliberately permits a late IPC response so production session guards are exercised. Successful verification establishes managed list/detail behavior with controlled native replies; actual white-ring detection, spatial candidate calculation and real-game button input require separate native and field validation. The 11 cursor scenarios, 25 default groups and five nearby chooser groups passed with the 2026-09-08 coordinator revision and zero build warnings/errors.

For a visual review, the following optional mode shows one controlled two-image guide for 60 seconds, then closes its windows and exits. It uses a separate log and does not overwrite regression results:

```powershell
$guidePreview = Start-Process -FilePath (Resolve-Path .\out\guide-window-runtime\GuideWindowRuntime.exe) -ArgumentList '--preview' -WindowStyle Hidden -PassThru
$guidePreview.Id
Get-Content .\out\guide-window-runtime\guide-window-preview.log
```

The preview's buttons operate on its fake services. Global keyboard hooks and game positioning are still simulated; `--preview` never starts CoreHost or interacts with a real game.
